{$ARXIV_SUMMARY_API_DOMAIN} {
    encode zstd gzip

    handle /api/* {
        reverse_proxy api:8000
    }

    handle /health {
        reverse_proxy api:8000
    }

    handle_path /paper/arxiv_summary/* {
        root * /srv/web
        try_files {path} /index.html
        file_server
    }

    handle_path /arxiv_summary/* {
        root * /srv/web
        try_files {path} /index.html
        file_server
    }

    redir / /paper/arxiv_summary/ 302
}
